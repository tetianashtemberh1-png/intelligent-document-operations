# Failure Handling

Implemented: validation, HTTP 400 response, arithmetic/business rules, explicit exception reasons, human-review routing.

Production: OCR timeout handling, retries/backoff, HTTP 429 handling, persistent idempotency, error queue, alerting, low-confidence fallback.
